import { SearchtermapiPipe } from './searchtermapi.pipe';

describe('SearchtermapiPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchtermapiPipe();
    expect(pipe).toBeTruthy();
  });
});
